# План доработки под ТЗ (NDPI Navbat) — Local-first

Архитектура: **локальный бокс на объекте работает офлайн по LAN, облако = админка + статистика, синк без конфликтов.**
Каждый шаг: **действие** → **критерий готово**. Отмечай `[x]` и иди дальше.

---

## Архитектура (зафиксировано)

```
        ОБЛАКО (nmpi.avtoxizmet.uz)                  ОБЪЕКТ (LAN)
  ┌──────────────────────────────┐          ┌──────────────────────────────┐
  │ Админка (откуда угодно, auth) │          │ Локальный бокс (NUC)          │
  │  • правит КАТАЛОГ + настройки │          │  Django+PG+Redis+nginx        │
  │  • аккаунты                   │          │  раздаёт киоск/табло/пульт    │
  │  • СТАТИСТИКА / отчёты / Excel │          │  по LAN, работает ОФЛАЙН      │
  │  • аудит                      │          │                               │
  └──────────────┬───────────────┘          └───────────────┬──────────────┘
                 │  catalog (cloud→local, pull)              │
                 │ ─────────────────────────────────────────►│  упорт в локальную БД
                 │                                           │
                 │  events (local→cloud, push)               │
                 │ ◄─────────────────────────────────────────│  талоны/сессии/аудит
  агрегация ◄────┘     (идемпотентно по UUID)                └─ киоск-ПК: агент печати (USB)
```

**Кто что пишет (нет конфликтов):**
- **Каталог** (Hall, Category, Service, Counter, User, Settings, Schedule) — пишет ТОЛЬКО облако → локальный читает.
- **События** (Ticket, OperatorSession, AuditLog, DailyCounter) — пишет ТОЛЬКО локальный → облако принимает и агрегирует.

**Офлайн:** локальный работает на закэшированном каталоге; обновления каталога подтягиваются, когда есть инет. Выдача/вызовы не страдают.
**Realtime удалённо** — отдельная будущая фаза (сейчас realtime только по LAN).

---

## Фаза A — Локальный деплой (фундамент)
Поднять текущий стек на локальном боксе, устройства по LAN. Без этого синк негде крутить.

- [ ] `deploy/docker-compose.local.yml` — стек без cloud-прокси: db+redis+backend+4 фронта+nginx, биндинг на LAN-интерфейс.
      **Готово:** на NUC `docker compose -f deploy/docker-compose.local.yml up -d` поднимает всё.
- [ ] Фронты собираются с `NEXT_PUBLIC_API` = адрес локального бокса (или relative через локальный nginx).
      **Готово:** киоск/табло/пульт открываются с других устройств LAN по `http://<box-ip>/`.
- [ ] Печать: агент на киоск-ПК (уже есть), киоск-страница шлёт на `localhost:8089` (CORS уже добавлен).
      **Готово:** талон печатается с киоск-ПК при бэке на NUC.
- [ ] Удалённый доступ к NUC для обслуживания (Tailscale/SSH).
      **Готово:** можно зайти на NUC удалённо, `docker compose pull && up`.

**Готово, когда:** вся система работает на NUC по LAN без интернета.

---

## Фаза B — Залы (1-зал / 2-зал) ✅ [ТЗ §2,4.4]
- [x] Модель `Hall` (catalog) + сид 2 залов + `GET /api/halls`.
- [x] FK `Counter.hall`, `ServiceCategory.hall`, `Ticket.hall`, `DailyCounter.hall` (+ миграции, текущие → 1-зал). Нумерация per-hall.
- [x] `services.*` (active/waiting/board) фильтруют по залу; call_next неявно scoped по услугам окна. pytest 26 зелёных (вкл. изоляцию залов).
- [x] display-эндпоинты `?hall_id=`. *(WS-группы по залу не делал — поллинг 2с + глобальный WS уже дают корректный per-hall рефетч; отдельные WS-группы = оптимизация, в бэклог.)*
- [x] Киоск: первый экран — выбор зала → категории этого зала (`?hall_id=`); «← Сменить зал»; зал в сторе.
- [x] Табло читает `?hall=` из URL, scoped запросы.
- [ ] *(мелочь)* Талон (экран + печать) показывает название зала — мини-доработка.

**Готово:** независимые очереди/табло на зал, киоск стартует с выбора зала, тесты+типы зелёные. Остался косметический показ зала на талоне.

---

## Фаза C — Auth и роли 🟡 [ТЗ §3,7.4]
- [x] Роли `chief_admin`/`hall_admin` (+legacy `admin`=chief) + `User.hall` + хелперы (Фаза C фундамент).
- [x] Permission-классы `IsChief`/`IsChiefOrReadOnly` навешены на **чувствительные эндпоинты**: аккаунты, настройки (PATCH), аудит, запись каталога/окон. Чтение/киоск/табло/оператор — публичны (default AllowAny). 32 теста (вкл. «без auth → 403»).
- [x] Админка шлёт JWT — fetch-перехватчик в `apps/admin` (вшивает Bearer в `/api/`).
- [ ] **Оператор-auth**: сейчас оператор без пароля/JWT (LoginScreen выбирает окно). Нужен пароль-логин оператора → затем защитить действия оператора. Отдельная задача.
- [ ] hall_admin scoping в querysets (видит только свой зал) — роль есть, фильтрация в вьюхах не сделана.
- [ ] Токен в WS.
- [ ] Смена дефолт-пароля admin/admin.

**Готово:** чувствительная админ-поверхность (аккаунты/настройки/аудит/каталог) под `IsChief`, админка авторизуется. Осталось: оператор-логин, hall-scoping querysets, WS-токен, смена пароля.

---

## Фаза D — Синк-движок (ядро local-first) 🟡 [новое]
- [x] `Ticket.finished_at` (Фаза E) + `updated_at` (auto_now) на Ticket/OperatorSession — watermark. Миграции.
- [x] **Cloud→local (каталог):** `GET /api/sync/catalog` — снапшот Hall/Category/Service/Counter/User/Settings.
- [x] **Local→cloud (события):** `POST /api/sync/events` — апсёрт tickets/sessions/audit по PK, **идемпотентно** (повтор не плодит). Тесты.
- [ ] **Локальные команды** `sync_pull` (тянет каталог → апсёрт в локальную БД) и `sync_push` (несинканые по watermark → POST). + модель `SyncState` (watermark).
- [ ] **Синк-воркер** (Celery-beat/loop) + бэкофф при офлайне.
- [ ] Конфиг `SYNC_ROLE`/`CLOUD_URL`/`SYNC_TOKEN` из env.

**Готово (облачная сторона):** эндпоинты снапшота и приёма событий, идемпотентно. Осталась локальная сторона (pull/push команды + worker) — её удобно делать вместе с **Фазой A** (локальный бокс), там и тестировать двумя инстансами.

---

## Фаза E — Статистика 🟡 [ТЗ §4.4,4.5]
- [x] `Ticket.finished_at` (+ проставляется в `finish`) + миграция.
- [x] `GET /api/stats?hall=&from=&to=`: обслужено, ср. ожидание (`called−created`), ср. обслуживание (`finished−called`), пропущено, **час пик**, hourly — по залу/диапазону.
- [x] Экспорт `GET /api/stats/export` → **CSV** (с BOM, открывается в Excel; .xlsx через `openpyxl` — позже одним `poetry add`). 29 тестов.
- [x] Дашборд в админке: секция «Статистика» с фильтром по залу + богатые метрики (выдано/обслужено/пропущено/ср.обслуживание/час пик) + кнопка CSV-экспорта.

**Готово:** метрики + дашборд + CSV-выгрузка. (.xlsx через `openpyxl` — опционально позже.)

---

## Фаза F — Голос (рус. TTS) ✅ [критерий приёмки, ТЗ §4.2]
- [x] `apps/display/lib/useSpeech.ts` — `useSpeech()` → «Номер A-042, пройдите к окну 3» (ru-RU), Web Speech API (`speechSynthesis`), после дин-дона.
- [x] Очередь: `speechSynthesis` сериализует фразы нативно (5 разом → по одной). Mute глушит дин-дон + голос (`cancelSpeech`).
- [ ] *(прод-качество, позже)* Подменить браузерный TTS на предзаписанные фрагменты (~35 клипов, склейка) — тот же call-site.

**Готово:** табло проговаривает номер+окно по-русски, в очередь, mute работает. *(Проверять на реальном Chrome/Edge с рус. голосом в ОС — в headless-превью звука нет.)*

---

## Фаза G — Аудит-журнал 🟡 [критерий приёмки, ТЗ §4.5,7.4]
- [x] Модель `AuditLog` (`actor, actor_label, action, target, meta, created_at, synced`) + миграция + Django-admin.
- [x] Хелпер `audit.log()` + `AuditCRUDMixin`; запись на call/finish/skip/transfer, **counter CRUD**, login.
- [x] `GET /api/audit?action=&actor=&from=&to=` (cap 200) + сериализатор. 27 тестов.
- [x] Страница `/audit` в админке: таблица (время/действие/объект/кто/детали) + фильтр по действию + пункт меню «Аудит».
- [ ] *(мелочь)* Аудит изменений **категорий/услуг** (в `catalog` — circular-import; добавить при заходе в catalog).

**Готово:** действия логируются + видны в админке с фильтрами. Критерий приёмки закрыт. Остался catalog-CRUD аудит (мелочь).

---

## Фаза H — Оператор: повтор/перерыв + мини-LED ✅ [ТЗ §4.3]
- [x] «Повторить вызов»: `POST /api/tickets/<id>/recall` (бампает `called_at`) → бродкаст `ticket.called`; табло переигрывает голос/вспышку (детекция свежести теперь по `called_at`). Кнопка в `CurrentTicket`. +аудит +тест.
- [x] «Перерыв»: уже было в `OperatorFooter` (`updateSession` status break/active). Перевод — тоже есть.
- [x] Мини-LED: `apps/display/app/led/page.tsx` — `/led?counter=<id>`, огромный номер на чёрном, обновление WS+поллинг.

**Готово:** повтор + перерыв + перевод у оператора, мини-LED показывает текущий номер окна. 28 тестов.

---

## Фаза I — Админ-экстра ❌ [ТЗ §4.4,4.5]
- [ ] Каталог редактируется в облачной админке (CRUD уже есть) — закрепить как авторитетный источник, добавить недостающее: персонал, **смены/расписание** (модель `WorkSchedule`).
      **Готово:** все справочники правятся в облаке, доезжают на локальный.
- [ ] Сброс/очистка очереди зала: `POST /api/halls/<id>/reset` (локально) + кнопка.
      **Готово:** очищает очередь зала, табло пустеет.
- [ ] Системные настройки (chief): расширить `DisplaySettings` → имя/лого/TTS вкл-язык-громкость/принтер/привязка табло-LED к окнам/залам.
      **Готово:** страница настроек правит, локальный/табло читают (через синк каталога).

**Готово, когда:** каталог/смены/настройки правятся в облаке, сброс очереди работает.

---

## Фаза J — Полиш
- [ ] Формат номера `A-042` (дефис) — `next_number` + печать + фронты.
- [ ] «Кол-во человек впереди» на талоне (опц., ТЗ §4.1).
- [ ] Английский язык киоска (ограниченно, будущее).

---

## Будущее (после сдачи)
- **Realtime-мониторинг очереди удалённо** — live-push состояния локального в облако, чтобы chief видел залы в реальном времени из любого места.

---

## Уже сделано ✅ (вне плана)
- Киоск: категория→услуга→подтверждение→печать; печать best-effort (талон в очереди даже без принтера); выбор принтера (`/settings`, 5 тапов, `GET /printers`).
- Агент печати: XP-80 (CP866), CORS, автозапуск (Планировщик), Edge-фолбэк, kiosk-host.
- Табло: YouTube + «Сейчас обслуживается» (вспышка) + лента окон + очередь ожидания, WS <1с.
- Бэк: Django DRF + Channels WS + Postgres; деплой Docker.
- ⚠️ деплой сейчас облачный — **Фаза A переносит операционку на локальный бокс**, облако остаётся под админку/статистику.

---

## Порядок исполнения
**A (локальный деплой) → B (залы) → C (auth) → D (синк) → E (статистика) → F (голос) → G (аудит) → H (оператор+LED) → I (админ-экстра) → J (полиш).**

Грубо: A ~2д, B ~3-4д, C ~2д, D (синк) ~3-4д, E ~2д, F ~0.5д, G ~1д, H ~1.5д, I ~3д, J ~1д. **Итого ~3 недели** (синк добавил ~неделю vs чисто облачного, но закрыл оффлайн как класс).
