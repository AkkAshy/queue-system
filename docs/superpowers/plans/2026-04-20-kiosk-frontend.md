# Kiosk Frontend (Phase 0 + Phase 1) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the monorepo skeleton and a working kiosk frontend on MSW mocks that lets a student pick a category → pick a service → confirm → receive a ticket number. No real backend, no real printer (mock).

**Architecture:** pnpm workspace + Turborepo monorepo. `apps/kiosk` is Next.js 15 (App Router, TS strict), styled with Tailwind + shadcn/ui via shared preset. `packages/types` holds domain types, `packages/mocks` holds MSW handlers and the 65-service fixture. State: Zustand local + TanStack Query for server state (MSW-backed). i18n: next-intl, locales `kaa` (default) + `ru`.

**Tech Stack:** pnpm 9, Turborepo 2, Next.js 15, React 19, TypeScript 5, Tailwind 3, shadcn/ui, TanStack Query 5, Zustand 5, MSW 2, next-intl 3, Vitest 2, React Testing Library, Playwright 1.

**Spec:** `docs/superpowers/specs/2026-04-20-queue-system-design.md`

---

## File Map

```
queue-system/
├── package.json                          # root workspace
├── pnpm-workspace.yaml
├── turbo.json
├── tsconfig.base.json                    # shared TS config
├── .eslintrc.json                        # shared ESLint
├── .prettierrc.json
├── .gitignore
├── .nvmrc
├── tailwind.preset.js                    # shared theme tokens
├── README.md
│
├── packages/
│   ├── types/
│   │   ├── package.json
│   │   ├── tsconfig.json
│   │   └── src/index.ts                  # ServiceCategory, Service, Ticket, DeliveryType
│   └── mocks/
│       ├── package.json
│       ├── tsconfig.json
│       ├── src/
│       │   ├── index.ts
│       │   ├── fixtures/
│       │   │   ├── categories.json       # 9 categories
│       │   │   └── services.json         # 65 services
│       │   ├── handlers.ts               # MSW request handlers
│       │   ├── browser.ts                # setupWorker (for apps)
│       │   └── ticket-store.ts           # in-memory counter per category
│       └── tests/handlers.test.ts
│
└── apps/kiosk/
    ├── package.json
    ├── next.config.mjs
    ├── tsconfig.json
    ├── tailwind.config.ts
    ├── postcss.config.js
    ├── components.json                   # shadcn
    ├── vitest.config.ts
    ├── playwright.config.ts
    ├── middleware.ts                     # next-intl
    ├── i18n/request.ts
    ├── messages/
    │   ├── kaa.json
    │   └── ru.json
    ├── public/
    │   └── mockServiceWorker.js          # generated by MSW
    ├── app/
    │   ├── layout.tsx                    # root, no locale
    │   ├── globals.css
    │   ├── providers.tsx
    │   └── [locale]/
    │       ├── layout.tsx                # locale layout + guards
    │       ├── page.tsx                  # main: category grid
    │       ├── category/[code]/page.tsx  # services list
    │       ├── confirm/page.tsx
    │       ├── ticket/page.tsx
    │       └── error/page.tsx
    ├── components/
    │   ├── CategoryCard.tsx
    │   ├── ServiceRow.tsx
    │   ├── LocaleSwitcher.tsx
    │   ├── KioskGuards.tsx
    │   └── IdleReset.tsx
    ├── lib/
    │   ├── printer.ts                    # mock printTicket()
    │   ├── query-client.ts
    │   └── msw.ts
    ├── store/
    │   └── kiosk-store.ts                # Zustand
    ├── hooks/
    │   └── use-idle-reset.ts
    └── tests/
        ├── printer.test.ts
        ├── use-idle-reset.test.tsx
        └── e2e/happy-path.spec.ts
```

Tasks below are ordered so each is self-contained. Commit after each task.

---

### Task 1: Initialize repo, pnpm workspace, git

**Files:**
- Create: `package.json`
- Create: `pnpm-workspace.yaml`
- Create: `.gitignore`
- Create: `.nvmrc`
- Create: `README.md` (stub)

- [ ] **Step 1: Create root `package.json`**

```json
{
  "name": "queue-system",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md,css}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,json,md,css}\""
  },
  "devDependencies": {
    "turbo": "^2.1.3",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20.10.0",
    "pnpm": ">=9.0.0"
  }
}
```

- [ ] **Step 2: Create `pnpm-workspace.yaml`**

```yaml
packages:
  - "apps/*"
  - "packages/*"
```

- [ ] **Step 3: Create `.gitignore`**

```gitignore
# deps
node_modules
.pnpm-store

# build
.next
dist
build
out
.turbo

# env
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# editor
.vscode
.idea
.DS_Store

# test
coverage
playwright-report
test-results
.nyc_output

# brainstorm sessions (mockups)
.superpowers

# MSW
public/mockServiceWorker.js
!**/apps/*/public/mockServiceWorker.js
```

- [ ] **Step 4: Create `.nvmrc`**

```
20.18.0
```

- [ ] **Step 5: Create stub `README.md`**

```markdown
# Queue System

Electronic queue management system for NDPI Registrator ofisi.

See `docs/superpowers/specs/2026-04-20-queue-system-design.md` for full design.

## Quick start

```bash
pnpm install
pnpm dev  # runs all apps
```

## Structure

- `apps/kiosk` — touch-screen kiosk for students (ticket issuing)
- `apps/admin` — admin panel (planned)
- `apps/operator` — operator console (planned)
- `apps/display` — waiting-hall display (planned)
- `packages/types` — shared TypeScript types
- `packages/mocks` — MSW handlers + fixtures
- `backend/` — Django 6 API (planned)
- `agent/` — Go local agent for printer/TTS (planned)
```

- [ ] **Step 6: Init git and commit**

```bash
cd /Users/akkanat/Projects/queue-system
git init
git add .
git commit -m "chore: initialize monorepo skeleton"
```

Expected: git repo created, one commit present. Run `git log --oneline` to verify.

---

### Task 2: Shared TypeScript, ESLint, Prettier configs

**Files:**
- Create: `tsconfig.base.json`
- Create: `.eslintrc.json`
- Create: `.prettierrc.json`
- Create: `.prettierignore`

- [ ] **Step 1: Create `tsconfig.base.json`**

```json
{
  "$schema": "https://json.schemastore.org/tsconfig",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,
    "allowSyntheticDefaultImports": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve"
  }
}
```

- [ ] **Step 2: Create `.eslintrc.json`**

```json
{
  "root": true,
  "extends": ["next/core-web-vitals", "prettier"],
  "rules": {
    "@typescript-eslint/no-unused-vars": ["warn", { "argsIgnorePattern": "^_" }],
    "react/no-unescaped-entities": "off"
  },
  "ignorePatterns": ["node_modules", ".next", "dist", ".turbo", "public/mockServiceWorker.js"]
}
```

- [ ] **Step 3: Create `.prettierrc.json`**

```json
{
  "singleQuote": true,
  "semi": true,
  "trailingComma": "all",
  "printWidth": 100,
  "tabWidth": 2,
  "arrowParens": "always",
  "plugins": ["prettier-plugin-tailwindcss"]
}
```

- [ ] **Step 4: Create `.prettierignore`**

```
node_modules
.next
dist
.turbo
pnpm-lock.yaml
public/mockServiceWorker.js
```

- [ ] **Step 5: Install ESLint + Prettier plugin at root**

```bash
pnpm add -Dw eslint eslint-config-next eslint-config-prettier prettier-plugin-tailwindcss
```

- [ ] **Step 6: Commit**

```bash
git add .
git commit -m "chore: add shared TS/ESLint/Prettier configs"
```

---

### Task 3: Turborepo config + shared Tailwind preset

**Files:**
- Create: `turbo.json`
- Create: `tailwind.preset.js`

- [ ] **Step 1: Create `turbo.json`**

```json
{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "lint": {
      "dependsOn": ["^build"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"]
    }
  }
}
```

- [ ] **Step 2: Create `tailwind.preset.js`**

```js
/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      colors: {
        // category palette (matches spec §3)
        category: {
          academic: '#60a5fa',    // A
          online: '#60a5fa',      // B
          hemis: '#a78bfa',       // C
          extra: '#fbbf24',       // D
          finance: '#34d399',     // E
          orders: '#f87171',      // F
          intl: '#fb7185',        // G
          science: '#22d3ee',     // H
          docs: '#e879f9',        // I
        },
      },
      fontSize: {
        // huge sizes for kiosk/display screens
        'kiosk-xl': ['6rem', { lineHeight: '1' }],
        'kiosk-lg': ['4rem', { lineHeight: '1.1' }],
        'kiosk-md': ['2rem', { lineHeight: '1.3' }],
      },
      fontFamily: {
        sans: ['Inter', 'system-ui', 'sans-serif'],
      },
    },
  },
};
```

- [ ] **Step 3: Commit**

```bash
git add turbo.json tailwind.preset.js
git commit -m "chore: add Turborepo config and shared Tailwind preset"
```

---

### Task 4: packages/types — shared domain types

**Files:**
- Create: `packages/types/package.json`
- Create: `packages/types/tsconfig.json`
- Create: `packages/types/src/index.ts`

- [ ] **Step 1: Create `packages/types/package.json`**

```json
{
  "name": "@queue/types",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
```

- [ ] **Step 2: Create `packages/types/tsconfig.json`**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": ["src/**/*"]
}
```

- [ ] **Step 3: Create `packages/types/src/index.ts`**

```ts
export type DeliveryType =
  | 'electron'
  | 'qagaz'
  | 'awizeki'
  | 'electron_qagaz'
  | 'electron_awizeki'
  | 'jiynalmali_papka';

export type TicketStatus =
  | 'waiting'
  | 'called'
  | 'serving'
  | 'served'
  | 'skipped'
  | 'cancelled';

export type UserRole = 'admin' | 'operator' | 'viewer';

export interface ServiceCategory {
  id: number;
  code: string;         // 'A', 'B', ...
  name_kaa: string;
  name_ru: string;
  color: string;        // hex
  order: number;
}

export interface Service {
  id: number;
  category_id: number;
  name_kaa: string;
  name_ru: string;
  sla_days: number;     // 0 = immediate
  delivery_type: DeliveryType;
  requires_visit: boolean;
  is_active: boolean;
}

export interface Ticket {
  id: string;           // uuid
  number: string;       // 'A042'
  category_id: number;
  service_id: number | null;
  status: TicketStatus;
  counter_id: number | null;
  created_at: string;   // ISO
}

export interface CreateTicketRequest {
  category_id: number;
  service_id?: number;
  idempotency_key: string;
}
```

- [ ] **Step 4: Verify typecheck**

```bash
cd packages/types
pnpm install
pnpm typecheck
```

Expected: no errors.

- [ ] **Step 5: Commit**

```bash
cd /Users/akkanat/Projects/queue-system
git add packages/types
git commit -m "feat(types): add shared domain types"
```

---

### Task 5: packages/mocks — fixtures (categories + 65 services)

**Files:**
- Create: `packages/mocks/package.json`
- Create: `packages/mocks/tsconfig.json`
- Create: `packages/mocks/src/fixtures/categories.json`
- Create: `packages/mocks/src/fixtures/services.json`

- [ ] **Step 1: Create `packages/mocks/package.json`**

```json
{
  "name": "@queue/mocks",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./browser": "./src/browser.ts",
    "./fixtures/categories": "./src/fixtures/categories.json",
    "./fixtures/services": "./src/fixtures/services.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "msw": "^2.6.0",
    "@queue/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.1"
  }
}
```

- [ ] **Step 2: Create `packages/mocks/tsconfig.json`**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": ["src/**/*"]
}
```

- [ ] **Step 3: Create `packages/mocks/src/fixtures/categories.json`**

```json
[
  { "id": 1, "code": "A", "name_kaa": "Akademiyalıq iskerlik", "name_ru": "Академическая деятельность", "color": "#60a5fa", "order": 1 },
  { "id": 2, "code": "B", "name_kaa": "Onlayn arza tapsırıw", "name_ru": "Подача онлайн-заявлений", "color": "#60a5fa", "order": 2 },
  { "id": 3, "code": "C", "name_kaa": "HEMIS", "name_ru": "HEMIS", "color": "#a78bfa", "order": 3 },
  { "id": 4, "code": "D", "name_kaa": "Qosımsha xızmetler", "name_ru": "Дополнительные услуги", "color": "#fbbf24", "order": 4 },
  { "id": 5, "code": "E", "name_kaa": "Buxgalteriya hám marketing", "name_ru": "Бухгалтерия и маркетинг", "color": "#34d399", "order": 5 },
  { "id": 6, "code": "F", "name_kaa": "Buyrıqlar / Akademiyalıq mobillik", "name_ru": "Приказы и академ. мобильность", "color": "#f87171", "order": 6 },
  { "id": 7, "code": "G", "name_kaa": "Xalıqaralıq baylanıslar", "name_ru": "Международные связи", "color": "#fb7185", "order": 7 },
  { "id": 8, "code": "H", "name_kaa": "Ilimiy iskerlik", "name_ru": "Научная деятельность", "color": "#22d3ee", "order": 8 },
  { "id": 9, "code": "I", "name_kaa": "Hújjetler", "name_ru": "Документы", "color": "#e879f9", "order": 9 }
]
```

- [ ] **Step 4: Create `packages/mocks/src/fixtures/services.json`**

Transcribed directly from `5. NDPI Registrator ofisida ko'rsatiladigan xizmatlar.PDF`. Use this exact content (65 services):

```json
[
  { "id": 1, "category_id": 1, "name_kaa": "Akademiyalıq maǵlıwmatnama hám transkript alıw", "name_ru": "Получение академической справки и транскрипта", "sla_days": 1, "delivery_type": "electron_qagaz", "requires_visit": true, "is_active": true },
  { "id": 2, "category_id": 1, "name_kaa": "Oqıw qaǵazın alıw", "name_ru": "Получение учебной карточки", "sla_days": 1, "delivery_type": "electron_qagaz", "requires_visit": true, "is_active": true },
  { "id": 3, "category_id": 1, "name_kaa": "Sabaq kesteleri haqqında maǵlıwmat alıw", "name_ru": "Получение информации о расписании", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 4, "category_id": 1, "name_kaa": "Imtixanlar dizimin alıw", "name_ru": "Получение списка экзаменов", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 5, "category_id": 1, "name_kaa": "Imtixan nátiyjelerin alıw", "name_ru": "Получение результатов экзаменов", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 6, "category_id": 1, "name_kaa": "GPA kórsetkishi haqqında maǵlıwmatnama alıw", "name_ru": "Получение справки о GPA", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 7, "category_id": 1, "name_kaa": "Ózlestiriw kórsetkishleri haqqında maǵlıwmat alıw", "name_ru": "Получение информации об успеваемости", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 8, "category_id": 1, "name_kaa": "Pán resursları boyınsha maǵlıwmat alıw", "name_ru": "Информация по предметным ресурсам", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 9, "category_id": 1, "name_kaa": "Oqıw baǵdarlamaları haqqında maǵlıwmat alıw", "name_ru": "Информация об учебных программах", "sla_days": 0, "delivery_type": "electron_awizeki", "requires_visit": true, "is_active": true },

  { "id": 10, "category_id": 2, "name_kaa": "Jataqxanaǵa waqtınsha jaylasıw arzası", "name_ru": "Заявление на временное размещение в общежитии", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 11, "category_id": 2, "name_kaa": "Oqıwdı kóshiriw hám qayta tiklew arzası", "name_ru": "Заявление на перевод и восстановление", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 12, "category_id": 2, "name_kaa": "Ekinshi tálimge kiriw — onlayn arza", "name_ru": "Онлайн-заявление на второе образование", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 13, "category_id": 2, "name_kaa": "Magistraturaǵa kiriw — onlayn arza", "name_ru": "Онлайн-заявление на магистратуру", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 14, "category_id": 2, "name_kaa": "Qayta oqıw ushın arza", "name_ru": "Заявление на повторное обучение", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 15, "category_id": 2, "name_kaa": "Jataqxanalarǵa jaylasıw arzası", "name_ru": "Заявление на размещение в общежитии", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },

  { "id": 16, "category_id": 3, "name_kaa": "Oqıw ornınan maǵlıwmatnama alıw", "name_ru": "Справка с места учёбы", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 17, "category_id": 3, "name_kaa": "Shaqırıw qaǵazın alıw (sırtqı/aralıqtan)", "name_ru": "Вызов на сессию (заочное/дистанционное)", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 18, "category_id": 3, "name_kaa": "Aylanba qaǵaz", "name_ru": "Обходной лист", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 19, "category_id": 3, "name_kaa": "Paroldı tiklew", "name_ru": "Восстановление пароля", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 20, "category_id": 3, "name_kaa": "Studentlik guwalıǵın alıw", "name_ru": "Получение студенческого билета", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 21, "category_id": 3, "name_kaa": "Platformaǵa kirgiziw hám toparǵa biriktiriw", "name_ru": "Добавление на платформу и в группу", "sla_days": 3, "delivery_type": "electron", "requires_visit": true, "is_active": true },
  { "id": 22, "category_id": 3, "name_kaa": "Tyutorlar kiritken maǵlıwmatlardı redaktorlaw", "name_ru": "Редактирование данных тьютора", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 23, "category_id": 3, "name_kaa": "Topardan toparǵa kóshirip ótkeriw (buyrıq tiykarında)", "name_ru": "Перевод из группы в группу (по приказу)", "sla_days": 0, "delivery_type": "electron", "requires_visit": true, "is_active": true },

  { "id": 24, "category_id": 4, "name_kaa": "Medicinalıq qamsızlandırıw alıw", "name_ru": "Медицинское страхование", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 25, "category_id": 4, "name_kaa": "JOO-daǵı xızmetler hám resurslar haqqında", "name_ru": "Информация об услугах и ресурсах ВУЗа", "sla_days": 0, "delivery_type": "awizeki", "requires_visit": true, "is_active": true },
  { "id": 26, "category_id": 4, "name_kaa": "Oqıwǵa qabıllaw hám kóshiriw boyınsha konsultaciya", "name_ru": "Консультация по приёму и переводу", "sla_days": 0, "delivery_type": "awizeki", "requires_visit": true, "is_active": true },
  { "id": 27, "category_id": 4, "name_kaa": "Socialıq qorǵaw reestri menen sinxronlastırıw", "name_ru": "Синхронизация с реестром соц. защиты", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 28, "category_id": 4, "name_kaa": "Qatnastı sebepli/sebepsiz kóshiriw", "name_ru": "Перенос пропусков (уважит./неуважит.)", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 29, "category_id": 4, "name_kaa": "Jeke grafik boyınsha pánlerdi ótew", "name_ru": "Индивидуальный график сдачи предметов", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 30, "category_id": 4, "name_kaa": "Imtixan nátiyjeleri boyınsha apellyaciya", "name_ru": "Апелляция по результатам экзамена", "sla_days": 0, "delivery_type": "electron_qagaz", "requires_visit": true, "is_active": true },

  { "id": 31, "category_id": 5, "name_kaa": "Tólem-shártnamasın alıw, qarızdarlıq haqqında", "name_ru": "Договор оплаты, информация о задолженности", "sla_days": 0, "delivery_type": "electron_awizeki", "requires_visit": true, "is_active": true },
  { "id": 32, "category_id": 5, "name_kaa": "Kredit shártnamasın alıw (qayta oqıwǵa)", "name_ru": "Кредитный договор (повторное обучение)", "sla_days": 0, "delivery_type": "electron_awizeki", "requires_visit": true, "is_active": true },
  { "id": 33, "category_id": 5, "name_kaa": "Jataqxanaǵa jaylasıw — arza hám shártnama", "name_ru": "Заявление и договор на общежитие", "sla_days": 0, "delivery_type": "electron_awizeki", "requires_visit": true, "is_active": true },
  { "id": 34, "category_id": 5, "name_kaa": "Stipendiya haqqında maǵlıwmatnama", "name_ru": "Справка о стипендии", "sla_days": 0, "delivery_type": "electron_awizeki", "requires_visit": true, "is_active": true },
  { "id": 35, "category_id": 5, "name_kaa": "Esap betin qálıplestirip alıw", "name_ru": "Формирование лицевого счёта", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 36, "category_id": 5, "name_kaa": "Ijara subsidiyası ushın arza", "name_ru": "Заявление на субсидию на аренду", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 37, "category_id": 5, "name_kaa": "Potencial jumıs beriwshiler dizimi", "name_ru": "Список потенциальных работодателей", "sla_days": 0, "delivery_type": "awizeki", "requires_visit": true, "is_active": true },
  { "id": 38, "category_id": 5, "name_kaa": "Pitkeriwshige jumıs ornı boyınsha maǵlıwmat", "name_ru": "Информация о месте работы (выпускнику)", "sla_days": 0, "delivery_type": "awizeki", "requires_visit": true, "is_active": true },
  { "id": 39, "category_id": 5, "name_kaa": "Jumısqa bólistiriw hújjetleri", "name_ru": "Документы распределения на работу", "sla_days": 0, "delivery_type": "awizeki", "requires_visit": true, "is_active": true },
  { "id": 40, "category_id": 5, "name_kaa": "Jumısqa jaylasıw boyınsha usınıslar", "name_ru": "Рекомендации по трудоустройству", "sla_days": 0, "delivery_type": "awizeki", "requires_visit": true, "is_active": true },

  { "id": 41, "category_id": 6, "name_kaa": "Buyrıqlardı rásmiylestiriw (shetletiw, kursta qaldırıw, kóshiriw, akad. dem alıs, tiklew)", "name_ru": "Оформление приказов (отчисление, оставление, перевод, академ. отпуск, восстановление)", "sla_days": 10, "delivery_type": "electron_qagaz", "requires_visit": true, "is_active": true },
  { "id": 42, "category_id": 6, "name_kaa": "Akademiyalıq mobillik boyınsha", "name_ru": "Академическая мобильность", "sla_days": 10, "delivery_type": "electron_qagaz", "requires_visit": true, "is_active": true },
  { "id": 43, "category_id": 6, "name_kaa": "Pánler parqın anıqlaw", "name_ru": "Определение разницы предметов", "sla_days": 3, "delivery_type": "electron_qagaz", "requires_visit": true, "is_active": true },

  { "id": 44, "category_id": 7, "name_kaa": "Oqıw ornınan inglis tilinde maǵlıwmatnama", "name_ru": "Справка с места учёбы на английском", "sla_days": 0, "delivery_type": "electron_qagaz", "requires_visit": true, "is_active": true },
  { "id": 45, "category_id": 7, "name_kaa": "Xalıqaralıq grantlar hám mobillik baǵdarlamaları", "name_ru": "Международные гранты и программы мобильности", "sla_days": 0, "delivery_type": "electron_qagaz", "requires_visit": true, "is_active": true },
  { "id": 46, "category_id": 7, "name_kaa": "Sırt el JOO'larında oqıwda konsultaciya", "name_ru": "Консультация по обучению в зарубежных ВУЗах", "sla_days": 0, "delivery_type": "awizeki", "requires_visit": true, "is_active": true },
  { "id": 47, "category_id": 7, "name_kaa": "Sırt elli studentlerge tólem-shártnama", "name_ru": "Договор оплаты для иностранных студентов", "sla_days": 0, "delivery_type": "electron_qagaz", "requires_visit": true, "is_active": true },
  { "id": 48, "category_id": 7, "name_kaa": "Sırt elli studentlerdi qabıllaw boyınsha", "name_ru": "Приём иностранных студентов", "sla_days": 0, "delivery_type": "electron_awizeki", "requires_visit": true, "is_active": true },
  { "id": 49, "category_id": 7, "name_kaa": "Sırt elli studentlerge oqıw processleri haqqında", "name_ru": "Инфо об учебном процессе иностр. студентам", "sla_days": 0, "delivery_type": "electron_awizeki", "requires_visit": true, "is_active": true },
  { "id": 50, "category_id": 7, "name_kaa": "Xalıqaralıq reytingler hám kelisimler", "name_ru": "Международные рейтинги и соглашения", "sla_days": 0, "delivery_type": "electron_awizeki", "requires_visit": true, "is_active": true },
  { "id": 51, "category_id": 7, "name_kaa": "Shet tillerin úyreniw kursları", "name_ru": "Курсы изучения иностранных языков", "sla_days": 0, "delivery_type": "electron_awizeki", "requires_visit": true, "is_active": true },

  { "id": 52, "category_id": 8, "name_kaa": "Grantlar hám tańlawlar haqqında", "name_ru": "Гранты и конкурсы", "sla_days": 0, "delivery_type": "electron_awizeki", "requires_visit": true, "is_active": true },
  { "id": 53, "category_id": 8, "name_kaa": "Ilimiy konferenciyalar", "name_ru": "Научные конференции", "sla_days": 0, "delivery_type": "electron_awizeki", "requires_visit": true, "is_active": true },
  { "id": 54, "category_id": 8, "name_kaa": "Innovaciyalıq ideyalar hám startaplar", "name_ru": "Инновации и стартапы", "sla_days": 0, "delivery_type": "electron_awizeki", "requires_visit": true, "is_active": true },
  { "id": 55, "category_id": 8, "name_kaa": "Atlı stipendiyalar", "name_ru": "Именные стипендии", "sla_days": 0, "delivery_type": "electron_awizeki", "requires_visit": true, "is_active": true },
  { "id": 56, "category_id": 8, "name_kaa": "Ilimiy joybarlar boyınsha konsultaciya", "name_ru": "Консультация по научным проектам", "sla_days": 0, "delivery_type": "awizeki", "requires_visit": true, "is_active": true },
  { "id": 57, "category_id": 8, "name_kaa": "Ustaz-shákirt mektebine aǵza bolıw", "name_ru": "Членство в школе наставник-ученик", "sla_days": 0, "delivery_type": "electron_awizeki", "requires_visit": true, "is_active": true },
  { "id": 58, "category_id": 8, "name_kaa": "Stipendiya hám grant tańlawları — arza", "name_ru": "Заявление на стипендию/грант", "sla_days": 0, "delivery_type": "electron_qagaz", "requires_visit": true, "is_active": true },

  { "id": 59, "category_id": 9, "name_kaa": "QR kodlı diplom alıw", "name_ru": "Получение диплома с QR-кодом", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 60, "category_id": 9, "name_kaa": "Buyrıqlardan kóshirme alıw", "name_ru": "Выписка из приказов", "sla_days": 10, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 61, "category_id": 9, "name_kaa": "Diplom hám diplomǵa qosımshasın alıw", "name_ru": "Диплом и приложение к диплому", "sla_days": 0, "delivery_type": "qagaz", "requires_visit": true, "is_active": true },
  { "id": 62, "category_id": 9, "name_kaa": "Diplom dublikatı ushın arza", "name_ru": "Заявление на дубликат диплома", "sla_days": 0, "delivery_type": "electron", "requires_visit": false, "is_active": true },
  { "id": 63, "category_id": 9, "name_kaa": "Diplom maǵlıwmatların qosıw/redaktorlaw", "name_ru": "Добавление/редактирование данных диплома", "sla_days": 0, "delivery_type": "electron_qagaz", "requires_visit": true, "is_active": true },
  { "id": 64, "category_id": 9, "name_kaa": "Hújjetlerdi tapsırıw", "name_ru": "Сдача документов", "sla_days": 0, "delivery_type": "qagaz", "requires_visit": true, "is_active": true },
  { "id": 65, "category_id": 9, "name_kaa": "Basqa JOO-ǵa kóshiriwde hújjetlerdi alıw", "name_ru": "Получение документов при переводе в другой ВУЗ", "sla_days": 0, "delivery_type": "jiynalmali_papka", "requires_visit": true, "is_active": true }
]
```

- [ ] **Step 5: Install and typecheck**

```bash
cd /Users/akkanat/Projects/queue-system
pnpm install
pnpm --filter @queue/mocks typecheck
```

Expected: no errors.

- [ ] **Step 6: Commit**

```bash
git add packages/mocks
git commit -m "feat(mocks): add categories and 65-service fixture from NDPI PDF"
```

---

### Task 6: MSW handlers with idempotent ticket creation

**Files:**
- Create: `packages/mocks/src/handlers.ts`
- Create: `packages/mocks/src/ticket-store.ts`
- Create: `packages/mocks/src/browser.ts`
- Create: `packages/mocks/src/index.ts`
- Create: `packages/mocks/tests/handlers.test.ts`

- [ ] **Step 1: Write the failing test** — `packages/mocks/tests/handlers.test.ts`

```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { TicketStore } from '../src/ticket-store';

describe('TicketStore', () => {
  let store: TicketStore;
  beforeEach(() => {
    store = new TicketStore();
  });

  it('generates sequential numbers per category prefix', () => {
    expect(store.create({ category_id: 1, code: 'A', idempotency_key: 'k1' }).number).toBe('A001');
    expect(store.create({ category_id: 1, code: 'A', idempotency_key: 'k2' }).number).toBe('A002');
    expect(store.create({ category_id: 5, code: 'E', idempotency_key: 'k3' }).number).toBe('E001');
  });

  it('returns the same ticket for duplicate idempotency key', () => {
    const first = store.create({ category_id: 1, code: 'A', idempotency_key: 'same' });
    const second = store.create({ category_id: 1, code: 'A', idempotency_key: 'same' });
    expect(second.id).toBe(first.id);
    expect(second.number).toBe(first.number);
  });

  it('pads numbers to 3 digits', () => {
    for (let i = 0; i < 9; i++) {
      store.create({ category_id: 1, code: 'A', idempotency_key: `k${i}` });
    }
    const t = store.create({ category_id: 1, code: 'A', idempotency_key: 'k-10' });
    expect(t.number).toBe('A010');
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd packages/mocks
pnpm test
```

Expected: FAIL with "Cannot find module '../src/ticket-store'".

- [ ] **Step 3: Implement `packages/mocks/src/ticket-store.ts`**

Note: uses global `crypto.randomUUID()` (available in Node 20+ and in browser/service-worker contexts — avoid `node:crypto` so the module runs inside the MSW service worker).

```ts
import type { Ticket } from '@queue/types';

interface CreateArgs {
  category_id: number;
  code: string;          // 'A', 'B', etc.
  service_id?: number;
  idempotency_key: string;
}

export class TicketStore {
  private counters = new Map<string, number>();      // code -> last number
  private byKey = new Map<string, Ticket>();         // idempotency_key -> ticket

  create(args: CreateArgs): Ticket {
    const cached = this.byKey.get(args.idempotency_key);
    if (cached) return cached;

    const last = this.counters.get(args.code) ?? 0;
    const next = last + 1;
    this.counters.set(args.code, next);

    const number = `${args.code}${next.toString().padStart(3, '0')}`;
    const ticket: Ticket = {
      id: crypto.randomUUID(),
      number,
      category_id: args.category_id,
      service_id: args.service_id ?? null,
      status: 'waiting',
      counter_id: null,
      created_at: new Date().toISOString(),
    };
    this.byKey.set(args.idempotency_key, ticket);
    return ticket;
  }

  reset() {
    this.counters.clear();
    this.byKey.clear();
  }
}
```

- [ ] **Step 4: Run test to verify it passes**

```bash
pnpm test
```

Expected: 3 tests pass.

- [ ] **Step 5: Implement `packages/mocks/src/handlers.ts`**

```ts
import { http, HttpResponse } from 'msw';
import categories from './fixtures/categories.json';
import services from './fixtures/services.json';
import { TicketStore } from './ticket-store';
import type { CreateTicketRequest, ServiceCategory } from '@queue/types';

const store = new TicketStore();

export const handlers = [
  http.get('/api/categories', () => HttpResponse.json(categories)),

  http.get('/api/services', ({ request }) => {
    const url = new URL(request.url);
    const cat = url.searchParams.get('category_id');
    const list = cat ? services.filter((s) => s.category_id === Number(cat)) : services;
    return HttpResponse.json(list);
  }),

  http.post('/api/tickets', async ({ request }) => {
    const body = (await request.json()) as CreateTicketRequest;
    const category = (categories as ServiceCategory[]).find((c) => c.id === body.category_id);
    if (!category) return HttpResponse.json({ error: 'unknown category' }, { status: 400 });

    // simulate small latency
    await new Promise((r) => setTimeout(r, 150));

    const ticket = store.create({
      category_id: body.category_id,
      code: category.code,
      service_id: body.service_id,
      idempotency_key: body.idempotency_key,
    });
    return HttpResponse.json(ticket, { status: 201 });
  }),
];
```

- [ ] **Step 6: Implement `packages/mocks/src/browser.ts`**

```ts
import { setupWorker } from 'msw/browser';
import { handlers } from './handlers';

export const worker = setupWorker(...handlers);
```

- [ ] **Step 7: Implement `packages/mocks/src/index.ts`**

```ts
export { handlers } from './handlers';
export { TicketStore } from './ticket-store';
export { default as categories } from './fixtures/categories.json';
export { default as services } from './fixtures/services.json';
```

- [ ] **Step 8: Run tests and typecheck**

```bash
pnpm test
pnpm typecheck
```

Expected: both green.

- [ ] **Step 9: Commit**

```bash
cd /Users/akkanat/Projects/queue-system
git add packages/mocks
git commit -m "feat(mocks): add MSW handlers with idempotent ticket creation"
```

---

### Task 7: Bootstrap apps/kiosk (Next.js 15)

**Files:**
- Create: `apps/kiosk/package.json`
- Create: `apps/kiosk/tsconfig.json`
- Create: `apps/kiosk/next.config.mjs`
- Create: `apps/kiosk/app/layout.tsx`
- Create: `apps/kiosk/app/globals.css`
- Create: `apps/kiosk/postcss.config.js`
- Create: `apps/kiosk/tailwind.config.ts`
- Create: `apps/kiosk/components.json`
- Create: `apps/kiosk/.eslintrc.json`

- [ ] **Step 1: Create `apps/kiosk/package.json`**

```json
{
  "name": "@queue/kiosk",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3001",
    "build": "next build",
    "start": "next start -p 3001",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "msw:init": "msw init public/ --save"
  },
  "dependencies": {
    "next": "15.0.0",
    "react": "19.0.0-rc.1",
    "react-dom": "19.0.0-rc.1",
    "@tanstack/react-query": "^5.59.0",
    "zustand": "^5.0.0",
    "next-intl": "^3.21.0",
    "msw": "^2.6.0",
    "clsx": "^2.1.1",
    "tailwind-merge": "^2.5.4",
    "class-variance-authority": "^0.7.0",
    "lucide-react": "^0.451.0",
    "@radix-ui/react-slot": "^1.1.0",
    "@queue/types": "workspace:*",
    "@queue/mocks": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "@types/react": "npm:types-react@rc",
    "@types/react-dom": "npm:types-react-dom@rc",
    "typescript": "^5.6.3",
    "tailwindcss": "^3.4.13",
    "postcss": "^8.4.47",
    "autoprefixer": "^10.4.20",
    "@tailwindcss/forms": "^0.5.9",
    "tailwindcss-animate": "^1.0.7",
    "eslint": "^8.57.1",
    "eslint-config-next": "15.0.0",
    "vitest": "^2.1.1",
    "@vitejs/plugin-react": "^4.3.2",
    "@testing-library/react": "^16.0.1",
    "@testing-library/user-event": "^14.5.2",
    "@testing-library/jest-dom": "^6.5.0",
    "jsdom": "^25.0.1",
    "@playwright/test": "^1.48.0"
  },
  "overrides": {
    "@types/react": "npm:types-react@rc",
    "@types/react-dom": "npm:types-react-dom@rc"
  }
}
```

- [ ] **Step 2: Create `apps/kiosk/tsconfig.json`**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./*"]
    },
    "noEmit": true,
    "incremental": true
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts"
  ],
  "exclude": ["node_modules"]
}
```

- [ ] **Step 3: Create `apps/kiosk/next.config.mjs`**

```js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  transpilePackages: ['@queue/types', '@queue/mocks'],
  experimental: {
    reactCompiler: false,
  },
};

export default nextConfig;
```

- [ ] **Step 4: Create `apps/kiosk/postcss.config.js`**

```js
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
```

- [ ] **Step 5: Create `apps/kiosk/tailwind.config.ts`**

```ts
import type { Config } from 'tailwindcss';
import preset from '../../tailwind.preset.js';

const config: Config = {
  presets: [preset],
  content: [
    './app/**/*.{ts,tsx}',
    './components/**/*.{ts,tsx}',
  ],
  plugins: [require('tailwindcss-animate')],
};

export default config;
```

- [ ] **Step 6: Create `apps/kiosk/components.json`** (shadcn config)

```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "default",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "tailwind.config.ts",
    "css": "app/globals.css",
    "baseColor": "slate",
    "cssVariables": true
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui"
  }
}
```

- [ ] **Step 7: Create `apps/kiosk/app/globals.css`**

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --background: 222 47% 11%;
    --foreground: 210 40% 98%;
    --card: 217 33% 17%;
    --card-foreground: 210 40% 98%;
    --primary: 217 91% 60%;
    --primary-foreground: 210 40% 98%;
    --muted: 217 33% 25%;
    --muted-foreground: 215 20% 70%;
    --border: 217 33% 25%;
    --radius: 1rem;
  }
  * { border-color: hsl(var(--border)); }
  html, body {
    height: 100%;
    background-color: hsl(var(--background));
    color: hsl(var(--foreground));
    user-select: none;
    -webkit-tap-highlight-color: transparent;
  }
}

@layer utilities {
  .touch-target { min-height: 80px; min-width: 80px; }
}
```

- [ ] **Step 8: Create `apps/kiosk/app/layout.tsx` (root, no locale — redirect handled by middleware)**

```tsx
import type { Metadata } from 'next';
import './globals.css';

export const metadata: Metadata = {
  title: 'Registrator ofisi — Kiosk',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
```

- [ ] **Step 9: Create `apps/kiosk/.eslintrc.json`**

```json
{
  "extends": "../../.eslintrc.json"
}
```

- [ ] **Step 10: Create `apps/kiosk/lib/utils.ts`** (shadcn expects this)

```ts
import { clsx, type ClassValue } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

- [ ] **Step 11: Install and build (sanity check)**

```bash
cd /Users/akkanat/Projects/queue-system
pnpm install
pnpm --filter @queue/kiosk typecheck
```

Expected: typecheck passes (app has no pages yet but config resolves).

- [ ] **Step 12: Commit**

```bash
git add apps/kiosk
git commit -m "feat(kiosk): bootstrap Next.js 15 app with Tailwind and shadcn config"
```

---

### Task 8: next-intl routing with kaa + ru

**Files:**
- Create: `apps/kiosk/i18n/request.ts`
- Create: `apps/kiosk/middleware.ts`
- Create: `apps/kiosk/messages/kaa.json`
- Create: `apps/kiosk/messages/ru.json`
- Create: `apps/kiosk/app/[locale]/layout.tsx`

- [ ] **Step 1: Create `apps/kiosk/i18n/request.ts`**

```ts
import { getRequestConfig } from 'next-intl/server';
import { notFound } from 'next/navigation';

export const locales = ['kaa', 'ru'] as const;
export const defaultLocale = 'kaa';
export type Locale = (typeof locales)[number];

export default getRequestConfig(async ({ locale }) => {
  if (!locales.includes(locale as Locale)) notFound();
  return {
    messages: (await import(`../messages/${locale}.json`)).default,
  };
});
```

- [ ] **Step 2: Create `apps/kiosk/middleware.ts`**

```ts
import createMiddleware from 'next-intl/middleware';
import { locales, defaultLocale } from './i18n/request';

export default createMiddleware({
  locales,
  defaultLocale,
  localePrefix: 'as-needed',
});

export const config = {
  matcher: ['/((?!api|_next|.*\\..*).*)'],
};
```

- [ ] **Step 3: Update `next.config.mjs` to use next-intl plugin**

Replace entire file:

```js
import createNextIntlPlugin from 'next-intl/plugin';

const withNextIntl = createNextIntlPlugin('./i18n/request.ts');

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  transpilePackages: ['@queue/types', '@queue/mocks'],
};

export default withNextIntl(nextConfig);
```

- [ ] **Step 4: Create `apps/kiosk/messages/kaa.json`**

```json
{
  "welcome": {
    "title": "Xosh kelipsiz",
    "subtitle": "Xızmettiń kategoriyasın tańlań"
  },
  "category": {
    "back": "Artqa",
    "selectService": "Xızmetti tańlań"
  },
  "confirm": {
    "title": "Tastıyıqlań",
    "service": "Xızmet",
    "category": "Kategoriya",
    "getTicket": "Talonnı alıw",
    "cancel": "Biykar etiw"
  },
  "ticket": {
    "title": "Siziń nomeriniz",
    "goTo": "Kútiw zalında ekranǵa qarań",
    "new": "Jańa talon"
  },
  "error": {
    "title": "Qáte júz berdi",
    "retry": "Qaytalań",
    "contact": "Operatorǵa múrájat etiń"
  },
  "locale": {
    "switch": "Русча"
  }
}
```

- [ ] **Step 5: Create `apps/kiosk/messages/ru.json`**

```json
{
  "welcome": {
    "title": "Добро пожаловать",
    "subtitle": "Выберите категорию услуги"
  },
  "category": {
    "back": "Назад",
    "selectService": "Выберите услугу"
  },
  "confirm": {
    "title": "Подтвердите",
    "service": "Услуга",
    "category": "Категория",
    "getTicket": "Получить талон",
    "cancel": "Отмена"
  },
  "ticket": {
    "title": "Ваш номер",
    "goTo": "Ждите вызова на табло",
    "new": "Новый талон"
  },
  "error": {
    "title": "Произошла ошибка",
    "retry": "Повторить",
    "contact": "Обратитесь к оператору"
  },
  "locale": {
    "switch": "Qaraqalpaqsha"
  }
}
```

- [ ] **Step 6: Create `apps/kiosk/app/[locale]/layout.tsx`**

```tsx
import { NextIntlClientProvider } from 'next-intl';
import { getMessages } from 'next-intl/server';
import { notFound } from 'next/navigation';
import { locales, type Locale } from '@/i18n/request';
import { Providers } from '../providers';

export function generateStaticParams() {
  return locales.map((locale) => ({ locale }));
}

export default async function LocaleLayout({
  children,
  params,
}: {
  children: React.ReactNode;
  params: Promise<{ locale: string }>;
}) {
  const { locale } = await params;
  if (!locales.includes(locale as Locale)) notFound();

  const messages = await getMessages();

  return (
    <html lang={locale}>
      <body className="min-h-screen antialiased">
        <NextIntlClientProvider messages={messages}>
          <Providers>{children}</Providers>
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

- [ ] **Step 7: Create `apps/kiosk/app/providers.tsx` (stub — will add QueryClient in Task 10)**

```tsx
'use client';

export function Providers({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
```

- [ ] **Step 8: Delete the root `app/layout.tsx` — now handled by `[locale]/layout.tsx`**

```bash
rm apps/kiosk/app/layout.tsx
```

- [ ] **Step 9: Create a stub home page** `apps/kiosk/app/[locale]/page.tsx`

```tsx
import { useTranslations } from 'next-intl';

export default function HomePage() {
  const t = useTranslations('welcome');
  return (
    <main className="flex min-h-screen items-center justify-center">
      <h1 className="text-kiosk-lg font-bold">{t('title')}</h1>
    </main>
  );
}
```

- [ ] **Step 10: Verify dev server**

```bash
cd apps/kiosk
pnpm dev
```

Open `http://localhost:3001/` → should redirect to `/kaa` (default locale) and show "Xosh kelipsiz".
Open `http://localhost:3001/ru` → should show "Добро пожаловать".

Ctrl-C to stop.

- [ ] **Step 11: Commit**

```bash
cd /Users/akkanat/Projects/queue-system
git add apps/kiosk
git commit -m "feat(kiosk): configure next-intl with kaa + ru locales"
```

---

### Task 9: Providers — TanStack Query + MSW in kiosk

**Files:**
- Modify: `apps/kiosk/app/providers.tsx`
- Create: `apps/kiosk/lib/query-client.ts`
- Create: `apps/kiosk/lib/msw.ts`

- [ ] **Step 1: Initialize MSW service worker file**

```bash
cd apps/kiosk
pnpm msw:init
```

This generates `public/mockServiceWorker.js`. Commit it (it's checked in — MSW requirement).

- [ ] **Step 2: Create `apps/kiosk/lib/query-client.ts`**

```ts
import { QueryClient } from '@tanstack/react-query';

export function makeQueryClient() {
  return new QueryClient({
    defaultOptions: {
      queries: {
        staleTime: 60_000,
        refetchOnWindowFocus: false,
        retry: 1,
      },
      mutations: {
        retry: 0,
      },
    },
  });
}

let browserClient: QueryClient | undefined;
export function getQueryClient() {
  if (typeof window === 'undefined') return makeQueryClient();
  if (!browserClient) browserClient = makeQueryClient();
  return browserClient;
}
```

- [ ] **Step 3: Create `apps/kiosk/lib/msw.ts`**

```ts
'use client';

let started = false;

export async function startMsw(): Promise<void> {
  if (started) return;
  if (typeof window === 'undefined') return;

  const { worker } = await import('@queue/mocks/browser');
  await worker.start({
    onUnhandledRequest: 'bypass',
    serviceWorker: { url: '/mockServiceWorker.js' },
  });
  started = true;
}
```

- [ ] **Step 4: Replace `apps/kiosk/app/providers.tsx`**

```tsx
'use client';

import { QueryClientProvider } from '@tanstack/react-query';
import { useEffect, useState } from 'react';
import { getQueryClient } from '@/lib/query-client';
import { startMsw } from '@/lib/msw';

export function Providers({ children }: { children: React.ReactNode }) {
  const [mswReady, setMswReady] = useState(process.env.NODE_ENV !== 'development');

  useEffect(() => {
    if (process.env.NODE_ENV === 'development') {
      startMsw().then(() => setMswReady(true));
    }
  }, []);

  const client = getQueryClient();

  if (!mswReady) {
    return (
      <div className="flex min-h-screen items-center justify-center text-kiosk-md">
        …
      </div>
    );
  }

  return <QueryClientProvider client={client}>{children}</QueryClientProvider>;
}
```

- [ ] **Step 5: Verify MSW intercepts a request**

```bash
pnpm dev
```

Open browser console at `http://localhost:3001/kaa`. Run in console:

```js
fetch('/api/categories').then((r) => r.json()).then(console.log);
```

Expected: array of 9 category objects printed.

Ctrl-C to stop.

- [ ] **Step 6: Commit**

```bash
cd /Users/akkanat/Projects/queue-system
git add apps/kiosk
git commit -m "feat(kiosk): wire TanStack Query + MSW in dev"
```

---

### Task 10: Zustand store

**Files:**
- Create: `apps/kiosk/store/kiosk-store.ts`

- [ ] **Step 1: Create `apps/kiosk/store/kiosk-store.ts`**

```ts
import { create } from 'zustand';
import type { ServiceCategory, Service, Ticket } from '@queue/types';

interface KioskState {
  category: ServiceCategory | null;
  service: Service | null;
  ticket: Ticket | null;
  idempotencyKey: string | null;

  setCategory: (c: ServiceCategory | null) => void;
  setService: (s: Service | null) => void;
  setTicket: (t: Ticket | null) => void;
  prepareIdempotencyKey: () => string;
  reset: () => void;
}

export const useKioskStore = create<KioskState>((set, get) => ({
  category: null,
  service: null,
  ticket: null,
  idempotencyKey: null,

  setCategory: (category) => set({ category }),
  setService: (service) => set({ service }),
  setTicket: (ticket) => set({ ticket }),

  prepareIdempotencyKey: () => {
    const existing = get().idempotencyKey;
    if (existing) return existing;
    const key = crypto.randomUUID();
    set({ idempotencyKey: key });
    return key;
  },

  reset: () => set({ category: null, service: null, ticket: null, idempotencyKey: null }),
}));
```

- [ ] **Step 2: Typecheck**

```bash
cd apps/kiosk
pnpm typecheck
```

Expected: no errors.

- [ ] **Step 3: Commit**

```bash
cd /Users/akkanat/Projects/queue-system
git add apps/kiosk/store
git commit -m "feat(kiosk): add Zustand store with idempotency key"
```

---

### Task 11: Main screen — category grid

**Files:**
- Create: `apps/kiosk/components/CategoryCard.tsx`
- Create: `apps/kiosk/components/LocaleSwitcher.tsx`
- Modify: `apps/kiosk/app/[locale]/page.tsx`

- [ ] **Step 1: Create `apps/kiosk/components/CategoryCard.tsx`**

```tsx
'use client';

import Link from 'next/link';
import { useLocale } from 'next-intl';
import type { ServiceCategory } from '@queue/types';
import { cn } from '@/lib/utils';

interface Props {
  category: ServiceCategory;
  onClick?: () => void;
}

export function CategoryCard({ category, onClick }: Props) {
  const locale = useLocale();
  const name = locale === 'ru' ? category.name_ru : category.name_kaa;

  return (
    <Link
      href={`/${locale}/category/${category.code}`}
      onClick={onClick}
      className={cn(
        'flex flex-col items-center justify-center gap-4 rounded-2xl p-8 text-center',
        'min-h-[220px] touch-target transition-transform active:scale-95',
        'border-4 hover:brightness-110',
      )}
      style={{ borderColor: category.color, backgroundColor: `${category.color}22` }}
    >
      <div
        className="flex h-24 w-24 items-center justify-center rounded-2xl font-bold text-kiosk-lg"
        style={{ backgroundColor: category.color, color: '#0f172a' }}
      >
        {category.code}
      </div>
      <div className="text-2xl font-semibold leading-tight">{name}</div>
    </Link>
  );
}
```

- [ ] **Step 2: Create `apps/kiosk/components/LocaleSwitcher.tsx`**

```tsx
'use client';

import { useLocale, useTranslations } from 'next-intl';
import { usePathname, useRouter } from 'next/navigation';

export function LocaleSwitcher() {
  const locale = useLocale();
  const t = useTranslations('locale');
  const router = useRouter();
  const pathname = usePathname();
  const next = locale === 'kaa' ? 'ru' : 'kaa';

  function switchTo() {
    const rest = pathname.replace(/^\/(kaa|ru)/, '');
    router.push(`/${next}${rest || '/'}`);
  }

  return (
    <button
      onClick={switchTo}
      className="rounded-xl bg-muted px-6 py-4 text-xl font-medium text-muted-foreground hover:bg-muted/80"
    >
      {t('switch')}
    </button>
  );
}
```

- [ ] **Step 3: Replace `apps/kiosk/app/[locale]/page.tsx`**

```tsx
'use client';

import { useQuery } from '@tanstack/react-query';
import { useTranslations } from 'next-intl';
import type { ServiceCategory } from '@queue/types';
import { CategoryCard } from '@/components/CategoryCard';
import { LocaleSwitcher } from '@/components/LocaleSwitcher';
import { useKioskStore } from '@/store/kiosk-store';

async function fetchCategories(): Promise<ServiceCategory[]> {
  const res = await fetch('/api/categories');
  if (!res.ok) throw new Error('Failed to load categories');
  return res.json();
}

export default function HomePage() {
  const t = useTranslations('welcome');
  const reset = useKioskStore((s) => s.reset);
  const { data, isLoading, isError } = useQuery({
    queryKey: ['categories'],
    queryFn: fetchCategories,
  });

  return (
    <main
      className="min-h-screen p-12"
      onMouseEnter={reset} // coming back to home resets flow state
    >
      <header className="mb-10 flex items-start justify-between">
        <div>
          <h1 className="text-kiosk-lg font-bold">{t('title')}</h1>
          <p className="mt-2 text-kiosk-md text-muted-foreground">{t('subtitle')}</p>
        </div>
        <LocaleSwitcher />
      </header>

      {isLoading && <div className="text-kiosk-md">…</div>}
      {isError && <div className="text-kiosk-md text-red-400">⚠</div>}

      {data && (
        <div className="grid grid-cols-3 gap-6">
          {data
            .sort((a, b) => a.order - b.order)
            .map((c) => (
              <CategoryCard key={c.id} category={c} />
            ))}
        </div>
      )}
    </main>
  );
}
```

- [ ] **Step 4: Verify in browser**

```bash
pnpm dev
```

Open `http://localhost:3001/`. You should see 9 coloured category cards with prefixes A–I. Click one — navigates to `/kaa/category/A` (404 for now, OK).

Ctrl-C.

- [ ] **Step 5: Commit**

```bash
cd /Users/akkanat/Projects/queue-system
git add apps/kiosk
git commit -m "feat(kiosk): add category grid on main screen with locale switcher"
```

---

### Task 12: Category → services list screen

**Files:**
- Create: `apps/kiosk/components/ServiceRow.tsx`
- Create: `apps/kiosk/app/[locale]/category/[code]/page.tsx`

- [ ] **Step 1: Create `apps/kiosk/components/ServiceRow.tsx`**

```tsx
'use client';

import { useLocale } from 'next-intl';
import type { Service, ServiceCategory } from '@queue/types';
import { cn } from '@/lib/utils';

interface Props {
  service: Service;
  category: ServiceCategory;
  onClick: () => void;
}

export function ServiceRow({ service, category, onClick }: Props) {
  const locale = useLocale();
  const name = locale === 'ru' ? service.name_ru : service.name_kaa;

  const disabled = !service.requires_visit;

  return (
    <button
      onClick={onClick}
      disabled={disabled}
      className={cn(
        'flex w-full items-center gap-6 rounded-2xl border-4 p-6 text-left transition-transform',
        'touch-target',
        disabled
          ? 'cursor-not-allowed border-muted bg-muted/30 opacity-50'
          : 'active:scale-[0.98] hover:brightness-110',
      )}
      style={
        disabled
          ? undefined
          : { borderColor: category.color, backgroundColor: `${category.color}18` }
      }
    >
      <div
        className="flex h-20 w-20 shrink-0 items-center justify-center rounded-xl font-bold text-kiosk-md"
        style={{ backgroundColor: category.color, color: '#0f172a' }}
      >
        {category.code}
      </div>
      <div className="flex-1">
        <div className="text-2xl font-semibold leading-snug">{name}</div>
        {disabled && (
          <div className="mt-1 text-base text-muted-foreground">
            {locale === 'ru' ? 'Доступно онлайн — HEMIS' : 'Onlayn — HEMIS'}
          </div>
        )}
      </div>
    </button>
  );
}
```

- [ ] **Step 2: Create `apps/kiosk/app/[locale]/category/[code]/page.tsx`**

```tsx
'use client';

import { useRouter, useParams } from 'next/navigation';
import { useQuery } from '@tanstack/react-query';
import { useLocale, useTranslations } from 'next-intl';
import type { Service, ServiceCategory } from '@queue/types';
import { ServiceRow } from '@/components/ServiceRow';
import { useKioskStore } from '@/store/kiosk-store';

async function fetchCategories(): Promise<ServiceCategory[]> {
  const res = await fetch('/api/categories');
  return res.json();
}

async function fetchServices(categoryId: number): Promise<Service[]> {
  const res = await fetch(`/api/services?category_id=${categoryId}`);
  return res.json();
}

export default function CategoryPage() {
  const params = useParams<{ code: string; locale: string }>();
  const t = useTranslations('category');
  const locale = useLocale();
  const router = useRouter();
  const { setCategory, setService } = useKioskStore();

  const { data: categories } = useQuery({
    queryKey: ['categories'],
    queryFn: fetchCategories,
  });
  const category = categories?.find((c) => c.code === params.code);

  const { data: services } = useQuery({
    queryKey: ['services', category?.id],
    queryFn: () => fetchServices(category!.id),
    enabled: !!category,
  });

  if (!category) return null;
  const categoryName = locale === 'ru' ? category.name_ru : category.name_kaa;

  return (
    <main className="min-h-screen p-12">
      <header className="mb-8 flex items-center gap-6">
        <button
          onClick={() => router.push(`/${locale}`)}
          className="rounded-xl bg-muted px-6 py-4 text-xl font-medium text-muted-foreground"
        >
          ← {t('back')}
        </button>
        <div
          className="flex h-16 w-16 items-center justify-center rounded-xl font-bold text-kiosk-md"
          style={{ backgroundColor: category.color, color: '#0f172a' }}
        >
          {category.code}
        </div>
        <h1 className="text-kiosk-md font-bold">{categoryName}</h1>
      </header>

      <div className="flex flex-col gap-4">
        {services?.map((s) => (
          <ServiceRow
            key={s.id}
            service={s}
            category={category}
            onClick={() => {
              setCategory(category);
              setService(s);
              router.push(`/${locale}/confirm`);
            }}
          />
        ))}
      </div>
    </main>
  );
}
```

- [ ] **Step 3: Verify in browser**

```bash
cd apps/kiosk && pnpm dev
```

Visit `/`. Click category "A" — see 9 services. Online-only services are greyed out. Click a visit-required service — navigates to `/kaa/confirm` (404 until next task).

Ctrl-C.

- [ ] **Step 4: Commit**

```bash
cd /Users/akkanat/Projects/queue-system
git add apps/kiosk
git commit -m "feat(kiosk): add services list page per category"
```

---

### Task 13: Mock printer with tests

**Files:**
- Create: `apps/kiosk/lib/printer.ts`
- Create: `apps/kiosk/vitest.config.ts`
- Create: `apps/kiosk/tests/printer.test.ts`

- [ ] **Step 1: Create `apps/kiosk/vitest.config.ts`**

```ts
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';
import path from 'node:path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, '.'),
    },
  },
  test: {
    environment: 'jsdom',
    setupFiles: [],
    include: ['tests/**/*.test.{ts,tsx}'],
    exclude: ['tests/e2e/**'],
  },
});
```

- [ ] **Step 2: Write the failing test `apps/kiosk/tests/printer.test.ts`**

```ts
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { printTicket } from '@/lib/printer';
import type { Ticket } from '@queue/types';

const sampleTicket: Ticket = {
  id: 't1',
  number: 'A042',
  category_id: 1,
  service_id: 5,
  status: 'waiting',
  counter_id: null,
  created_at: new Date().toISOString(),
};

describe('printTicket (mock)', () => {
  beforeEach(() => vi.useFakeTimers());
  afterEach(() => vi.useRealTimers());

  it('resolves with ok: true after delay', async () => {
    const logSpy = vi.spyOn(console, 'log').mockImplementation(() => {});
    const promise = printTicket(sampleTicket);
    await vi.advanceTimersByTimeAsync(500);
    await expect(promise).resolves.toEqual({ ok: true });
    expect(logSpy).toHaveBeenCalledWith('[mock-printer]', 'A042');
    logSpy.mockRestore();
  });

  it('returns ok: false when FORCE_PRINTER_FAIL is set', async () => {
    process.env.NEXT_PUBLIC_FORCE_PRINTER_FAIL = '1';
    const promise = printTicket(sampleTicket);
    await vi.advanceTimersByTimeAsync(500);
    const result = await promise;
    expect(result.ok).toBe(false);
    delete process.env.NEXT_PUBLIC_FORCE_PRINTER_FAIL;
  });
});
```

- [ ] **Step 3: Run to verify failure**

```bash
cd apps/kiosk
pnpm test
```

Expected: FAIL with "Cannot find module '@/lib/printer'".

- [ ] **Step 4: Implement `apps/kiosk/lib/printer.ts`**

```ts
import type { Ticket } from '@queue/types';

export interface PrintResult {
  ok: boolean;
  error?: string;
}

/**
 * Mock printer. Phase 2 will replace this with a fetch to
 * http://localhost:8089/print (Go agent + Xprinter XP-80T).
 */
export async function printTicket(ticket: Ticket): Promise<PrintResult> {
  await new Promise((r) => setTimeout(r, 500));

  if (process.env.NEXT_PUBLIC_FORCE_PRINTER_FAIL === '1') {
    return { ok: false, error: 'forced failure' };
  }

  // eslint-disable-next-line no-console
  console.log('[mock-printer]', ticket.number);
  return { ok: true };
}
```

- [ ] **Step 5: Verify tests pass**

```bash
pnpm test
```

Expected: 2 tests pass.

- [ ] **Step 6: Commit**

```bash
cd /Users/akkanat/Projects/queue-system
git add apps/kiosk
git commit -m "feat(kiosk): add mock printer with vitest coverage"
```

---

### Task 14: Confirmation page + idempotent ticket mutation

**Files:**
- Create: `apps/kiosk/app/[locale]/confirm/page.tsx`

- [ ] **Step 1: Create `apps/kiosk/app/[locale]/confirm/page.tsx`**

```tsx
'use client';

import { useRouter } from 'next/navigation';
import { useMutation } from '@tanstack/react-query';
import { useLocale, useTranslations } from 'next-intl';
import type { Ticket } from '@queue/types';
import { useKioskStore } from '@/store/kiosk-store';
import { printTicket } from '@/lib/printer';

async function createTicket(body: {
  category_id: number;
  service_id: number;
  idempotency_key: string;
}): Promise<Ticket> {
  const res = await fetch('/api/tickets', {
    method: 'POST',
    headers: { 'content-type': 'application/json' },
    body: JSON.stringify(body),
  });
  if (!res.ok) throw new Error('ticket creation failed');
  return res.json();
}

export default function ConfirmPage() {
  const t = useTranslations('confirm');
  const locale = useLocale();
  const router = useRouter();
  const { category, service, prepareIdempotencyKey, setTicket } = useKioskStore();

  const mutation = useMutation({
    mutationFn: createTicket,
    onSuccess: async (ticket) => {
      setTicket(ticket);
      const result = await printTicket(ticket);
      if (!result.ok) {
        router.push(`/${locale}/error`);
        return;
      }
      router.push(`/${locale}/ticket`);
    },
    onError: () => router.push(`/${locale}/error`),
  });

  if (!category || !service) {
    if (typeof window !== 'undefined') router.push(`/${locale}`);
    return null;
  }

  const serviceName = locale === 'ru' ? service.name_ru : service.name_kaa;
  const categoryName = locale === 'ru' ? category.name_ru : category.name_kaa;

  const confirm = () => {
    const key = prepareIdempotencyKey();
    mutation.mutate({ category_id: category.id, service_id: service.id, idempotency_key: key });
  };

  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-10 p-12">
      <h1 className="text-kiosk-lg font-bold">{t('title')}</h1>

      <div className="w-full max-w-3xl space-y-6 rounded-3xl border-4 bg-card p-10 text-center"
           style={{ borderColor: category.color }}>
        <div>
          <div className="text-muted-foreground">{t('category')}</div>
          <div className="text-kiosk-md font-semibold">{categoryName}</div>
        </div>
        <div>
          <div className="text-muted-foreground">{t('service')}</div>
          <div className="text-2xl leading-snug">{serviceName}</div>
        </div>
      </div>

      <div className="flex w-full max-w-3xl gap-6">
        <button
          className="flex-1 rounded-2xl bg-muted py-8 text-kiosk-md font-semibold"
          onClick={() => router.push(`/${locale}`)}
          disabled={mutation.isPending}
        >
          {t('cancel')}
        </button>
        <button
          className="flex-1 rounded-2xl py-8 text-kiosk-md font-semibold text-slate-900"
          style={{ backgroundColor: category.color }}
          onClick={confirm}
          disabled={mutation.isPending}
        >
          {mutation.isPending ? '…' : t('getTicket')}
        </button>
      </div>
    </main>
  );
}
```

- [ ] **Step 2: Verify**

```bash
pnpm dev
```

Flow: `/` → category A → click "Akademiyalıq maǵlıwmatnama..." → `/kaa/confirm` shows category + service → click Получить талон → navigates to `/kaa/ticket` (404 until next task). Network tab shows `POST /api/tickets` with 201 response. Console shows `[mock-printer] A001`.

Ctrl-C.

- [ ] **Step 3: Commit**

```bash
cd /Users/akkanat/Projects/queue-system
git add apps/kiosk
git commit -m "feat(kiosk): add confirmation screen with idempotent ticket creation"
```

---

### Task 15: Ticket-issued page + error page

**Files:**
- Create: `apps/kiosk/app/[locale]/ticket/page.tsx`
- Create: `apps/kiosk/app/[locale]/error/page.tsx`

- [ ] **Step 1: Create `apps/kiosk/app/[locale]/ticket/page.tsx`**

```tsx
'use client';

import { useRouter } from 'next/navigation';
import { useEffect } from 'react';
import { useLocale, useTranslations } from 'next-intl';
import { useKioskStore } from '@/store/kiosk-store';

export default function TicketPage() {
  const t = useTranslations('ticket');
  const locale = useLocale();
  const router = useRouter();
  const { ticket, category, reset } = useKioskStore();

  useEffect(() => {
    if (!ticket) router.replace(`/${locale}`);
  }, [ticket, locale, router]);

  if (!ticket || !category) return null;

  const goHome = () => {
    reset();
    router.push(`/${locale}`);
  };

  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-12 p-12">
      <div className="text-kiosk-md text-muted-foreground">{t('title')}</div>
      <div
        className="flex h-[380px] w-[380px] items-center justify-center rounded-[3rem] font-black"
        style={{ backgroundColor: category.color, color: '#0f172a', fontSize: '10rem', lineHeight: 1 }}
      >
        {ticket.number}
      </div>
      <div className="max-w-3xl text-center text-kiosk-md">{t('goTo')}</div>
      <button
        onClick={goHome}
        className="rounded-2xl bg-muted px-12 py-6 text-kiosk-md font-semibold"
      >
        {t('new')}
      </button>
    </main>
  );
}
```

- [ ] **Step 2: Create `apps/kiosk/app/[locale]/error/page.tsx`**

```tsx
'use client';

import { useRouter } from 'next/navigation';
import { useLocale, useTranslations } from 'next-intl';
import { useKioskStore } from '@/store/kiosk-store';

export default function ErrorPage() {
  const t = useTranslations('error');
  const locale = useLocale();
  const router = useRouter();
  const reset = useKioskStore((s) => s.reset);

  const retry = () => {
    reset();
    router.push(`/${locale}`);
  };

  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-10 p-12">
      <div className="text-[8rem]">⚠</div>
      <h1 className="text-kiosk-lg font-bold text-red-400">{t('title')}</h1>
      <p className="text-kiosk-md text-muted-foreground">{t('contact')}</p>
      <button
        onClick={retry}
        className="rounded-2xl bg-primary px-12 py-6 text-kiosk-md font-semibold text-primary-foreground"
      >
        {t('retry')}
      </button>
    </main>
  );
}
```

- [ ] **Step 3: Verify full flow**

```bash
pnpm dev
```

Flow: `/` → category → service → confirm → ticket screen shows big number like "A001". Click "Jańa talon" — goes back home. Click another service — now "A002". Refresh confirm page mid-flow → redirects home (store cleared).

Test error flow — set env var and restart:

```bash
NEXT_PUBLIC_FORCE_PRINTER_FAIL=1 pnpm dev
```

Complete flow → should land on `/kaa/error` after printer "fails".

Ctrl-C.

- [ ] **Step 4: Commit**

```bash
cd /Users/akkanat/Projects/queue-system
git add apps/kiosk
git commit -m "feat(kiosk): add ticket-issued and printer-error screens"
```

---

### Task 16: Idle reset hook (with test)

**Files:**
- Create: `apps/kiosk/hooks/use-idle-reset.ts`
- Create: `apps/kiosk/components/IdleReset.tsx`
- Create: `apps/kiosk/tests/use-idle-reset.test.tsx`
- Modify: `apps/kiosk/app/[locale]/layout.tsx`

- [ ] **Step 1: Write failing test** — `apps/kiosk/tests/use-idle-reset.test.tsx`

```tsx
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { renderHook, act } from '@testing-library/react';
import { useIdleReset } from '@/hooks/use-idle-reset';

describe('useIdleReset', () => {
  beforeEach(() => vi.useFakeTimers());
  afterEach(() => vi.useRealTimers());

  it('fires callback after timeout when no activity', () => {
    const cb = vi.fn();
    renderHook(() => useIdleReset({ timeoutMs: 1000, onIdle: cb, enabled: true }));
    act(() => {
      vi.advanceTimersByTime(1000);
    });
    expect(cb).toHaveBeenCalledTimes(1);
  });

  it('does not fire when disabled', () => {
    const cb = vi.fn();
    renderHook(() => useIdleReset({ timeoutMs: 1000, onIdle: cb, enabled: false }));
    act(() => {
      vi.advanceTimersByTime(2000);
    });
    expect(cb).not.toHaveBeenCalled();
  });

  it('resets the timer on pointerdown', () => {
    const cb = vi.fn();
    renderHook(() => useIdleReset({ timeoutMs: 1000, onIdle: cb, enabled: true }));
    act(() => {
      vi.advanceTimersByTime(700);
      window.dispatchEvent(new PointerEvent('pointerdown'));
      vi.advanceTimersByTime(700);
    });
    expect(cb).not.toHaveBeenCalled();
    act(() => {
      vi.advanceTimersByTime(400);
    });
    expect(cb).toHaveBeenCalledTimes(1);
  });
});
```

- [ ] **Step 2: Run, verify failure**

```bash
cd apps/kiosk && pnpm test
```

Expected: FAIL — module not found.

- [ ] **Step 3: Implement `apps/kiosk/hooks/use-idle-reset.ts`**

```ts
import { useEffect, useRef } from 'react';

interface Options {
  timeoutMs: number;
  onIdle: () => void;
  enabled: boolean;
}

const ACTIVITY_EVENTS = ['pointerdown', 'keydown', 'touchstart'];

export function useIdleReset({ timeoutMs, onIdle, enabled }: Options) {
  const cbRef = useRef(onIdle);
  cbRef.current = onIdle;

  useEffect(() => {
    if (!enabled) return;
    let timer = setTimeout(() => cbRef.current(), timeoutMs);

    const reset = () => {
      clearTimeout(timer);
      timer = setTimeout(() => cbRef.current(), timeoutMs);
    };

    ACTIVITY_EVENTS.forEach((e) => window.addEventListener(e, reset, { passive: true }));
    return () => {
      clearTimeout(timer);
      ACTIVITY_EVENTS.forEach((e) => window.removeEventListener(e, reset));
    };
  }, [timeoutMs, enabled]);
}
```

- [ ] **Step 4: Implement `apps/kiosk/components/IdleReset.tsx`**

```tsx
'use client';

import { usePathname, useRouter } from 'next/navigation';
import { useLocale } from 'next-intl';
import { useIdleReset } from '@/hooks/use-idle-reset';
import { useKioskStore } from '@/store/kiosk-store';

const TIMEOUT_MS = 30_000;

export function IdleReset() {
  const pathname = usePathname();
  const locale = useLocale();
  const router = useRouter();
  const reset = useKioskStore((s) => s.reset);

  // Only active on non-home pages
  const enabled = !(pathname === `/${locale}` || pathname === `/`);

  useIdleReset({
    timeoutMs: TIMEOUT_MS,
    enabled,
    onIdle: () => {
      reset();
      router.push(`/${locale}`);
    },
  });

  return null;
}
```

- [ ] **Step 5: Modify `apps/kiosk/app/[locale]/layout.tsx`** — mount `IdleReset`

In the `<body>` element, add `<IdleReset />` alongside children:

```tsx
import { IdleReset } from '@/components/IdleReset';
// ...
return (
  <html lang={locale}>
    <body className="min-h-screen antialiased">
      <NextIntlClientProvider messages={messages}>
        <Providers>
          <IdleReset />
          {children}
        </Providers>
      </NextIntlClientProvider>
    </body>
  </html>
);
```

- [ ] **Step 6: Run tests**

```bash
pnpm test
```

Expected: all tests pass.

- [ ] **Step 7: Commit**

```bash
cd /Users/akkanat/Projects/queue-system
git add apps/kiosk
git commit -m "feat(kiosk): add 30s idle reset with tested hook"
```

---

### Task 17: Kiosk guards (no context menu, no F11, no text selection)

**Files:**
- Create: `apps/kiosk/components/KioskGuards.tsx`
- Modify: `apps/kiosk/app/[locale]/layout.tsx`

- [ ] **Step 1: Create `apps/kiosk/components/KioskGuards.tsx`**

```tsx
'use client';

import { useEffect } from 'react';

/**
 * Blocks context menu, text selection, and common escape keys.
 * Does NOT exit fullscreen — that's done by the browser's kiosk-mode flag.
 */
export function KioskGuards() {
  useEffect(() => {
    const prevent = (e: Event) => e.preventDefault();

    const keyHandler = (e: KeyboardEvent) => {
      // F5, F11, F12, Ctrl+R, Ctrl+Shift+I, Ctrl+U, Alt+F4
      const block =
        ['F5', 'F11', 'F12'].includes(e.key) ||
        (e.ctrlKey && ['r', 'R', 'u', 'U'].includes(e.key)) ||
        (e.ctrlKey && e.shiftKey && ['i', 'I', 'j', 'J'].includes(e.key)) ||
        (e.altKey && e.key === 'F4');
      if (block) e.preventDefault();
    };

    document.addEventListener('contextmenu', prevent);
    document.addEventListener('selectstart', prevent);
    document.addEventListener('dragstart', prevent);
    document.addEventListener('keydown', keyHandler);
    return () => {
      document.removeEventListener('contextmenu', prevent);
      document.removeEventListener('selectstart', prevent);
      document.removeEventListener('dragstart', prevent);
      document.removeEventListener('keydown', keyHandler);
    };
  }, []);

  return null;
}
```

- [ ] **Step 2: Mount in `apps/kiosk/app/[locale]/layout.tsx`**

Next to `<IdleReset />`:

```tsx
import { KioskGuards } from '@/components/KioskGuards';
// ...
<Providers>
  <KioskGuards />
  <IdleReset />
  {children}
</Providers>
```

- [ ] **Step 3: Verify in browser**

```bash
pnpm dev
```

Right-click → no menu. Try to select text → nothing selects. Press F12 → does not open devtools (note: in dev, browsers might still honour F12; this is best-effort — real kiosk mode comes from Chrome `--kiosk` flag).

Ctrl-C.

- [ ] **Step 4: Commit**

```bash
cd /Users/akkanat/Projects/queue-system
git add apps/kiosk
git commit -m "feat(kiosk): add UI guards (no context menu, no F5/F11/F12)"
```

---

### Task 18: Playwright E2E — happy path

**Files:**
- Create: `apps/kiosk/playwright.config.ts`
- Create: `apps/kiosk/tests/e2e/happy-path.spec.ts`

- [ ] **Step 1: Install Playwright browsers**

```bash
cd apps/kiosk
pnpm exec playwright install chromium
```

- [ ] **Step 2: Create `apps/kiosk/playwright.config.ts`**

```ts
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './tests/e2e',
  timeout: 30_000,
  reporter: [['list']],
  use: {
    baseURL: 'http://localhost:3001',
    trace: 'on-first-retry',
  },
  webServer: {
    command: 'pnpm dev',
    url: 'http://localhost:3001',
    reuseExistingServer: !process.env.CI,
    timeout: 60_000,
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
  ],
});
```

- [ ] **Step 3: Create `apps/kiosk/tests/e2e/happy-path.spec.ts`**

```ts
import { test, expect } from '@playwright/test';

test('student picks a service and gets a ticket number', async ({ page }) => {
  await page.goto('/kaa');

  // Main screen shows 9 category cards
  await expect(page.getByRole('heading', { name: 'Xosh kelipsiz' })).toBeVisible();
  const categoryA = page.locator('a', { hasText: 'Akademiyalıq iskerlik' });
  await expect(categoryA).toBeVisible();
  await categoryA.click();

  // Services list
  await expect(page.locator('text=Akademiyalıq maǵlıwmatnama').first()).toBeVisible();
  await page.locator('button', { hasText: 'Akademiyalıq maǵlıwmatnama' }).first().click();

  // Confirm screen
  await expect(page.getByRole('heading', { name: 'Tastıyıqlań' })).toBeVisible();
  await page.getByRole('button', { name: 'Talonnı alıw' }).click();

  // Ticket screen — expect number starting with A
  await expect(page.locator('text=/^A\\d{3}$/')).toBeVisible({ timeout: 5000 });
});

test('idle timer returns to home from category page', async ({ page }) => {
  await page.goto('/kaa');
  await page.locator('a', { hasText: 'Akademiyalıq iskerlik' }).click();
  await expect(page.locator('text=Akademiyalıq maǵlıwmatnama').first()).toBeVisible();

  // Fast-forward fake idle: we can't easily forward timers through Next,
  // so verify the hook exists by waiting >30s would be slow. Instead
  // just ensure navigation works when the store is reset:
  await page.goto('/kaa');
  await expect(page.getByRole('heading', { name: 'Xosh kelipsiz' })).toBeVisible();
});
```

- [ ] **Step 4: Add `test:e2e` script if not present** — already in package.json as `playwright test`.

- [ ] **Step 5: Run E2E**

```bash
cd apps/kiosk
pnpm test:e2e
```

Expected: both tests pass. If webServer boot exceeds timeout, increase `timeout` in config or start `pnpm dev` manually in another shell.

- [ ] **Step 6: Commit**

```bash
cd /Users/akkanat/Projects/queue-system
git add apps/kiosk
git commit -m "test(kiosk): add Playwright happy-path E2E"
```

---

### Task 19: Final README + verification

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Replace `README.md`**

```markdown
# Queue System

Electronic queue management system for **NDPI Registrator ofisi** (Nukus State Pedagogical Institute — Registrar's Office).

Design: [`docs/superpowers/specs/2026-04-20-queue-system-design.md`](docs/superpowers/specs/2026-04-20-queue-system-design.md)

## Status

- ✅ Phase 0 — monorepo skeleton, 65-service fixture
- ✅ Phase 1 — kiosk frontend on mocks
- ⏳ Phase 2 — Go agent + Xprinter XP-80T
- ⏳ Phase 3 — admin on mocks
- ⏳ Phase 4 — operator console on mocks
- ⏳ Phase 5 — display (табло) on mocks
- ⏳ Phase 6 — Django 6 API (replace mocks)
- ⏳ Phase 7 — realtime (Channels + Redis)
- ⏳ Phase 8 — deploy

## Structure

```
apps/
  kiosk/       # touch-screen kiosk (Next.js 15, built)
  admin/       # admin panel (planned)
  operator/    # operator console (planned)
  display/     # waiting-hall display (planned)
packages/
  types/       # shared TS types
  mocks/       # MSW handlers + fixture (65 services, 9 categories)
backend/       # Django 6 API (planned)
agent/         # Go local agent (planned)
```

## Prerequisites

- Node.js 20+ and pnpm 9+
- (Phase 2+) Go 1.22+
- (Phase 6+) Python 3.12, Poetry, PostgreSQL 16, Redis 7

## Quick start

```bash
pnpm install
pnpm --filter @queue/kiosk dev   # http://localhost:3001
```

Default locale is `kaa` (Karakalpak). Switch to `ru` via the top-right button.

## Scripts

```bash
pnpm dev               # all apps in parallel (Turborepo)
pnpm build             # production builds
pnpm lint              # lint all packages
pnpm typecheck         # typecheck all packages
pnpm test              # unit tests
pnpm --filter @queue/kiosk test:e2e   # Playwright happy path
pnpm format            # prettier
```

## Kiosk flow

1. Main screen: 9 category cards (colour-coded A–I)
2. Pick a category → list of services in that category
3. Pick a service (online-only are shown disabled with "HEMIS" hint)
4. Confirm → mock `POST /api/tickets` (MSW) + mock print → ticket screen
5. Ticket screen shows big number (e.g. `A042`) with counter-direction prompt
6. 30 s of inactivity on any non-home screen → auto-return to home
```

- [ ] **Step 2: Run everything as final check**

```bash
cd /Users/akkanat/Projects/queue-system
pnpm install
pnpm typecheck
pnpm lint
pnpm test
```

Expected: all green across all packages.

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "docs: finalize README for phase 0+1 completion"
```

---

## Verification checklist (manual, on real hardware — optional)

- [ ] `pnpm --filter @queue/kiosk dev` starts without errors.
- [ ] Visiting `/` redirects to `/kaa` and shows 9 category cards.
- [ ] Picking category "A" shows 9 services with correct `requires_visit=false` disabled state (services 3–8 are greyed).
- [ ] Picking a visit-required service → confirm screen shows correct service + category name.
- [ ] Confirm → ticket screen shows `A001` first time, `A002` second time (counter in-memory).
- [ ] Double-click "Получить талон" on confirm screen doesn't create two tickets (idempotency key).
- [ ] Switch locale ru ↔ kaa — state persists, translations switch.
- [ ] Sit idle on confirm screen 30 s → returns to home, state cleared.
- [ ] Right-click anywhere → no menu.
- [ ] `NEXT_PUBLIC_FORCE_PRINTER_FAIL=1 pnpm dev` → flow ends on `/kaa/error`.
